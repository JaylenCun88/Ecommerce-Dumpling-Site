# Supabase values from the project Connect dialog.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Test-mode Stripe credentials. Never prefix these server-only keys with NEXT_PUBLIC_.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Used only by the verified Stripe webhook to create orders.
SUPABASE_SERVICE_ROLE_KEY=
